# storm-tv

Public hosting mirror of the **Storm TV** Cast receiver (`index.html`), served via GitHub
Pages so a Chromecast can load it as a custom receiver. Source of truth is the private
`storm` repo's `cast-receiver/`. No secrets here — it's a generic message-driven canvas.
